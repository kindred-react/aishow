# 数据结构与算法面试 40 题

> 基于数据结构与算法知识整理，覆盖数组字符串、链表、树、排序搜索、动态规划、图及手撕代码

---

## 目录

- [一、数组与字符串（8题）](#一数组与字符串)
- [二、链表（6题）](#二链表)
- [三、树与二叉树（8题）](#三树与二叉树)
- [四、排序与搜索（6题）](#四排序与搜索)
- [五、动态规划（6题）](#五动态规划)
- [六、图与 BFS/DFS（3题）](#六图与-bfsdfs)
- [七、手撕代码（3题）](#七手撕代码)
- [高频考点速查表](#高频考点速查表)

---

## 一、数组与字符串

### Q1: 两数之和（LeetCode 167）

**难度**: ⭐

**答案**: 使用对撞指针，左指针从头开始，右指针从尾开始，根据和与目标值的关系移动指针。

**解析**:
```javascript
function twoSum(numbers, target) {
  let left = 0, right = numbers.length - 1;
  while (left < right) {
    const sum = numbers[left] + numbers[right];
    if (sum === target) return [left + 1, right + 1];
    if (sum < target) left++;
    else right--;
  }
  return [-1, -1];
}
// 时间复杂度: O(n)，空间复杂度: O(1)
```

---

### Q2: 盛最多水的容器（LeetCode 11）

**难度**: ⭐⭐

**答案**: 对撞指针，每次移动较短的一边（因为移动较长边不可能增加面积）。

**解析**:
```javascript
function maxArea(height) {
  let left = 0, right = height.length - 1, maxWater = 0;
  while (left < right) {
    const width = right - left;
    const h = Math.min(height[left], height[right]);
    maxWater = Math.max(maxWater, width * h);
    if (height[left] < height[right]) left++;
    else right--;
  }
  return maxWater;
}
// 时间复杂度: O(n)，空间复杂度: O(1)
```

---

### Q3: 三数之和（LeetCode 15）

**难度**: ⭐⭐

**答案**: 排序 + 固定一个数 + 双指针找另外两个数，注意跳过重复元素。

**解析**:
```javascript
function threeSum(nums) {
  const result = [];
  nums.sort((a, b) => a - b);
  for (let i = 0; i < nums.length - 2; i++) {
    if (i > 0 && nums[i] === nums[i - 1]) continue;
    if (nums[i] > 0) break;
    let left = i + 1, right = nums.length - 1;
    while (left < right) {
      const sum = nums[i] + nums[left] + nums[right];
      if (sum === 0) {
        result.push([nums[i], nums[left], nums[right]]);
        while (left < right && nums[left] === nums[left + 1]) left++;
        while (left < right && nums[right] === nums[right - 1]) right--;
        left++; right--;
      } else if (sum < 0) left++;
      else right--;
    }
  }
  return result;
}
// 时间复杂度: O(n^2)，空间复杂度: O(1)
```

---

### Q4: 无重复字符的最长子串（LeetCode 3）

**难度**: ⭐⭐

**答案**: 滑动窗口 + 哈希表记录字符最新位置，遇到重复字符时收缩左边界。

**解析**:
```javascript
function lengthOfLongestSubstring(s) {
  const charIndex = new Map();
  let left = 0, maxLen = 0;
  for (let right = 0; right < s.length; right++) {
    const char = s[right];
    if (charIndex.has(char) && charIndex.get(char) >= left) {
      left = charIndex.get(char) + 1;
    }
    charIndex.set(char, right);
    maxLen = Math.max(maxLen, right - left + 1);
  }
  return maxLen;
}
// 时间复杂度: O(n)，空间复杂度: O(min(m,n))
```

---

### Q5: 最小覆盖子串（LeetCode 76）

**难度**: ⭐⭐⭐

**答案**: 滑动窗口，用哈希表记录需要的字符和窗口内已有的字符，收缩窗口直到满足条件。

**解析**:
```javascript
function minWindow(s, t) {
  if (t.length > s.length) return "";
  const need = new Map(), window = new Map();
  for (const c of t) need.set(c, (need.get(c) || 0) + 1);
  let left = 0, right = 0, valid = 0, start = 0, minLen = Infinity;
  while (right < s.length) {
    const c = s[right++];
    if (need.has(c)) {
      window.set(c, (window.get(c) || 0) + 1);
      if (window.get(c) === need.get(c)) valid++;
    }
    while (valid === need.size) {
      if (right - left < minLen) { start = left; minLen = right - left; }
      const d = s[left++];
      if (need.has(d)) {
        if (window.get(d) === need.get(d)) valid--;
        window.set(d, window.get(d) - 1);
      }
    }
  }
  return minLen === Infinity ? "" : s.substring(start, start + minLen);
}
```

---

### Q6: 和为 K 的子数组（LeetCode 560）

**难度**: ⭐⭐

**答案**: 前缀和 + 哈希表。若 `prefix[j] - prefix[i] = k`，则子数组 `[i, j-1]` 的和为 k。

**解析**:
```javascript
function subarraySum(nums, k) {
  const prefixCount = new Map();
  prefixCount.set(0, 1);
  let prefixSum = 0, count = 0;
  for (const num of nums) {
    prefixSum += num;
    if (prefixCount.has(prefixSum - k)) {
      count += prefixCount.get(prefixSum - k);
    }
    prefixCount.set(prefixSum, (prefixCount.get(prefixSum) || 0) + 1);
  }
  return count;
}
// 时间复杂度: O(n)，空间复杂度: O(n)
```

---

### Q7: 接雨水（LeetCode 42）

**难度**: ⭐⭐⭐

**答案**: 预处理每个位置的左右最大高度，当前位置的雨水量 = min(左最大, 右最大) - 当前高度。

**解析**:
```javascript
function trap(height) {
  const n = height.length;
  const leftMax = new Array(n).fill(0);
  const rightMax = new Array(n).fill(0);
  leftMax[0] = height[0];
  for (let i = 1; i < n; i++) leftMax[i] = Math.max(leftMax[i-1], height[i]);
  rightMax[n-1] = height[n-1];
  for (let i = n - 2; i >= 0; i--) rightMax[i] = Math.max(rightMax[i+1], height[i]);
  let water = 0;
  for (let i = 0; i < n; i++) water += Math.min(leftMax[i], rightMax[i]) - height[i];
  return water;
}
// 时间复杂度: O(n)，空间复杂度: O(n)
```

---

### Q8: 最长回文子串（LeetCode 5）

**难度**: ⭐⭐⭐

**答案**: 中心扩展法，以每个字符（或字符间隙）为中心向两边扩展，记录最长回文。

**解析**:
```javascript
function longestPalindrome(s) {
  let result = '';
  for (let i = 0; i < s.length; i++) {
    expand(s, i, i);     // 奇数长度回文
    expand(s, i, i + 1); // 偶数长度回文
  }
  function expand(s, left, right) {
    while (left >= 0 && right < s.length && s[left] === s[right]) {
      left--; right++;
    }
    const sub = s.substring(left + 1, right);
    if (sub.length > result.length) result = sub;
  }
  return result;
}
// 时间复杂度: O(n^2)，空间复杂度: O(1)
```

---

## 二、链表

### Q9: 反转链表（LeetCode 206）

**难度**: ⭐

**答案**: 迭代法：三个指针（prev、curr、next）逐个反转。递归法：递归到末尾后逐个反转。

**解析**:
```javascript
// 迭代法（推荐）
function reverseList(head) {
  let prev = null, curr = head;
  while (curr) {
    const next = curr.next;
    curr.next = prev;
    prev = curr;
    curr = next;
  }
  return prev;
}
// 时间复杂度: O(n)，空间复杂度: O(1)

// 递归法
function reverseList(head) {
  if (!head || !head.next) return head;
  const newHead = reverseList(head.next);
  head.next.next = head;
  head.next = null;
  return newHead;
}
// 时间复杂度: O(n)，空间复杂度: O(n)（递归栈）
```

---

### Q10: 合并两个有序链表（LeetCode 21）

**难度**: ⭐

**答案**: 使用哑节点简化头节点处理，逐个比较两个链表的节点。

**解析**:
```javascript
function mergeTwoLists(l1, l2) {
  const dummy = new ListNode(0);
  let curr = dummy;
  while (l1 && l2) {
    if (l1.val <= l2.val) { curr.next = l1; l1 = l1.next; }
    else { curr.next = l2; l2 = l2.next; }
    curr = curr.next;
  }
  curr.next = l1 || l2;
  return dummy.next;
}
// 时间复杂度: O(m+n)，空间复杂度: O(1)
```

---

### Q11: 链表中环的检测（LeetCode 141）

**难度**: ⭐⭐

**答案**: 快慢指针法。快指针每次走两步，慢指针每次走一步，如果有环，快慢指针会相遇。

**解析**:
```javascript
function hasCycle(head) {
  let slow = head, fast = head;
  while (fast && fast.next) {
    slow = slow.next;
    fast = fast.next.next;
    if (slow === fast) return true;
  }
  return false;
}
// 时间复杂度: O(n)，空间复杂度: O(1)

// 数学证明：
// 设环前长度为 a，环长度为 b
// 相遇时：fast 走了 a + n*b + c，slow 走了 a + c
// fast = 2 * slow → a + n*b + c = 2(a + c) → a = n*b - c
// 即：从 head 走 a 步 = 从相遇点走 n*b - c 步
```

---

### Q12: 环形链表 II — 找到环的入口（LeetCode 142）

**难度**: ⭐⭐⭐

**答案**: 快慢指针相遇后，将一个指针移到头节点，两个指针以相同速度前进，再次相遇即为入口。

**解析**:
```javascript
function detectCycle(head) {
  let slow = head, fast = head;
  while (fast && fast.next) {
    slow = slow.next;
    fast = fast.next.next;
    if (slow === fast) {
      slow = head;
      while (slow !== fast) {
        slow = slow.next;
        fast = fast.next;
      }
      return slow;
    }
  }
  return null;
}
```

---

### Q13: 删除链表的倒数第 N 个节点（LeetCode 19）

**难度**: ⭐⭐

**答案**: 快慢指针法。快指针先走 N 步，然后快慢一起走，快指针到达末尾时，慢指针在倒数第 N+1 个节点。

**解析**:
```javascript
function removeNthFromEnd(head, n) {
  const dummy = new ListNode(0, head);
  let fast = dummy, slow = dummy;
  for (let i = 0; i <= n; i++) fast = fast.next;
  while (fast) {
    fast = fast.next;
    slow = slow.next;
  }
  slow.next = slow.next.next;
  return dummy.next;
}
// 时间复杂度: O(n)，空间复杂度: O(1)
```

---

### Q14: 合并 K 个升序链表（LeetCode 23）

**难度**: ⭐⭐⭐

**答案**: 使用优先队列（最小堆），每次取最小的链表头节点。

**解析**:
```javascript
function mergeKLists(lists) {
  // 使用最小堆（优先队列），按节点值排序
  // 注意：JavaScript 没有内置优先队列，实际项目中可使用第三方库如 heapq
  // 这里用数组模拟最小堆，并实现 heapifyUp 和 extractMin

  // 上浮操作：将索引 i 的元素上移到正确位置
  function heapifyUp(heap, i) {
    while (i > 0) {
      const parent = Math.floor((i - 1) / 2);
      if (heap[parent].val <= heap[i].val) break;
      [heap[parent], heap[i]] = [heap[i], heap[parent]];
      i = parent;
    }
  }

  // 下沉操作：将索引 i 的元素下沉到正确位置
  function heapifyDown(heap, i, size) {
    while (true) {
      let smallest = i;
      const left = 2 * i + 1;
      const right = 2 * i + 2;
      if (left < size && heap[left].val < heap[smallest].val) smallest = left;
      if (right < size && heap[right].val < heap[smallest].val) smallest = right;
      if (smallest === i) break;
      [heap[smallest], heap[i]] = [heap[i], heap[smallest]];
      i = smallest;
    }
  }

  // 弹出堆顶最小元素
  function extractMin(heap) {
    const min = heap[0];
    const last = heap.pop();
    if (heap.length > 0) {
      heap[0] = last;
      heapifyDown(heap, 0, heap.length);
    }
    return min;
  }

  // 构建初始堆
  const heap = [];
  for (const list of lists) {
    if (list) {
      heap.push(list);
      heapifyUp(heap, heap.length - 1);
    }
  }
  const dummy = new ListNode(0);
  let curr = dummy;
  while (heap.length > 0) {
    const node = extractMin(heap);
    curr.next = node;
    curr = curr.next;
    if (node.next) {
      heap.push(node.next);
      heapifyUp(heap, heap.length - 1);
    }
  }
  return dummy.next;
}
// 时间复杂度: O(N log K)，空间复杂度: O(K)
```

---

## 三、树与二叉树

### Q15: 二叉树的前序/中序/后序遍历

**难度**: ⭐⭐

**答案**: 前序（根-左-右）、中序（左-根-右）、后序（左-右-根）。递归和迭代两种实现。

**解析**:
```javascript
// 前序遍历（递归）
function preorder(root, result = []) {
  if (!root) return result;
  result.push(root.val);
  preorder(root.left, result);
  preorder(root.right, result);
  return result;
}

// 中序遍历（迭代）
function inorder(root) {
  const result = [], stack = [];
  let curr = root;
  while (curr || stack.length) {
    while (curr) { stack.push(curr); curr = curr.left; }
    curr = stack.pop();
    result.push(curr.val);
    curr = curr.right;
  }
  return result;
}

// 层序遍历（BFS）
function levelOrder(root) {
  if (!root) return [];
  const result = [], queue = [root];
  while (queue.length) {
    const level = [];
    const size = queue.length;
    for (let i = 0; i < size; i++) {
      const node = queue.shift();
      level.push(node.val);
      if (node.left) queue.push(node.left);
      if (node.right) queue.push(node.right);
    }
    result.push(level);
  }
  return result;
}
```

---

### Q16: 二叉树的最大深度（LeetCode 104）

**难度**: ⭐

**答案**: 递归：`maxDepth = 1 + max(leftDepth, rightDepth)`。迭代：层序遍历计数层数。

**解析**:
```javascript
function maxDepth(root) {
  if (!root) return 0;
  return 1 + Math.max(maxDepth(root.left), maxDepth(root.right));
}
// 时间复杂度: O(n)，空间复杂度: O(h)，h 为树高
```

---

### Q17: 验证二叉搜索树（LeetCode 98）

**难度**: ⭐⭐

**答案**: 中序遍历 BST 得到有序数组，检查遍历结果是否严格递增。或递归检查每个节点是否在有效范围内。

**解析**:
```javascript
function isValidBST(root, min = -Infinity, max = Infinity) {
  if (!root) return true;
  if (root.val <= min || root.val >= max) return false;
  return isValidBST(root.left, min, root.val) &&
         isValidBST(root.right, root.val, max);
}
// 时间复杂度: O(n)，空间复杂度: O(h)
```

---

### Q18: 二叉树的最近公共祖先（LeetCode 236）

**难度**: ⭐⭐

**答案**: 递归查找，如果当前节点是 p 或 q，返回当前节点。如果左右子树都找到了，当前节点就是 LCA。

**解析**:
```javascript
function lowestCommonAncestor(root, p, q) {
  if (!root || root === p || root === q) return root;
  const left = lowestCommonAncestor(root.left, p, q);
  const right = lowestCommonAncestor(root.right, p, q);
  if (left && right) return root; // p 和 q 分别在左右子树
  return left || right; // 都在左子树或右子树
}
```

---

### Q19: 路径总和（LeetCode 112）

**难度**: ⭐⭐

**答案**: DFS 递归，每层减去当前节点值，到叶子节点时检查是否等于 0。

**解析**:
```javascript
function hasPathSum(root, targetSum) {
  if (!root) return false;
  if (!root.left && !root.right) return root.val === targetSum;
  return hasPathSum(root.left, targetSum - root.val) ||
         hasPathSum(root.right, targetSum - root.val);
}
```

---

### Q20: 二叉搜索树的第 K 小元素（LeetCode 230）

**难度**: ⭐⭐

**答案**: 中序遍历 BST，第 K 个访问的节点即为答案。

**解析**:
```javascript
function kthSmallest(root, k) {
  const stack = [];
  let curr = root;
  while (curr || stack.length) {
    while (curr) { stack.push(curr); curr = curr.left; }
    curr = stack.pop();
    if (--k === 0) return curr.val;
    curr = curr.right;
  }
}
// 时间复杂度: O(H + K)，空间复杂度: O(H)
```

---

### Q21: 从前序与中序遍历构造二叉树（LeetCode 105）

**难度**: ⭐⭐⭐

**答案**: 前序第一个元素是根节点，在中序中找到根节点位置，左边是左子树，右边是右子树，递归构建。

**解析**:
```javascript
function buildTree(preorder, inorder) {
  const map = new Map();
  inorder.forEach((val, i) => map.set(val, i));
  function build(preStart, preEnd, inStart, inEnd) {
    if (preStart > preEnd) return null;
    const rootVal = preorder[preStart];
    const root = new TreeNode(rootVal);
    const inIndex = map.get(rootVal);
    const leftSize = inIndex - inStart;
    root.left = build(preStart + 1, preStart + leftSize, inStart, inIndex - 1);
    root.right = build(preStart + leftSize + 1, preEnd, inIndex + 1, inEnd);
    return root;
  }
  return build(0, preorder.length - 1, 0, inorder.length - 1);
}
// 时间复杂度: O(n)，空间复杂度: O(n)
```

---

### Q22: 翻转二叉树（LeetCode 226）

**难度**: ⭐

**答案**: 递归交换每个节点的左右子树。

**解析**:
```javascript
function invertTree(root) {
  if (!root) return null;
  [root.left, root.right] = [invertTree(root.right), invertTree(root.left)];
  return root;
}
```

---

## 四、排序与搜索

### Q23: 快速排序的实现和复杂度？

**难度**: ⭐⭐

**答案**: 选择基准元素，将数组分为小于和大于基准的两部分，递归排序。平均 O(n log n)，最坏 O(n^2)。

**解析**:
```javascript
function quickSort(arr, left = 0, right = arr.length - 1) {
  if (left >= right) return arr;
  const pivotIndex = partition(arr, left, right);
  quickSort(arr, left, pivotIndex - 1);
  quickSort(arr, pivotIndex + 1, right);
  return arr;
}

function partition(arr, left, right) {
  const pivot = arr[right];
  let i = left;
  for (let j = left; j < right; j++) {
    if (arr[j] < pivot) {
      [arr[i], arr[j]] = [arr[j], arr[i]];
      i++;
    }
  }
  [arr[i], arr[right]] = [arr[right], arr[i]];
  return i;
}
// 优化：三数取中法选择 pivot，避免最坏情况
```

---

### Q24: 归并排序的实现和复杂度？

**难度**: ⭐⭐

**答案**: 分治思想，将数组分成两半分别排序，然后合并。时间复杂度 O(n log n)，空间复杂度 O(n)，稳定排序。

**解析**:
```javascript
function mergeSort(arr) {
  if (arr.length <= 1) return arr;
  const mid = Math.floor(arr.length / 2);
  const left = mergeSort(arr.slice(0, mid));
  const right = mergeSort(arr.slice(mid));
  return merge(left, right);
}

function merge(left, right) {
  const result = [];
  let i = 0, j = 0;
  while (i < left.length && j < right.length) {
    if (left[i] <= right[j]) result.push(left[i++]);
    else result.push(right[j++]);
  }
  return result.concat(left.slice(i)).concat(right.slice(j));
}
```

---

### Q25: 二分查找的实现和变体？

**难度**: ⭐⭐

**答案**: 在有序数组中，每次将搜索范围缩小一半。变体：查找第一个/最后一个等于目标值的元素。

**解析**:
```javascript
// 基本二分查找
function binarySearch(nums, target) {
  let left = 0, right = nums.length - 1;
  while (left <= right) {
    const mid = Math.floor((left + right) / 2);
    if (nums[mid] === target) return mid;
    if (nums[mid] < target) left = mid + 1;
    else right = mid - 1;
  }
  return -1;
}

// 查找第一个 >= target 的位置（lower_bound）
function lowerBound(nums, target) {
  let left = 0, right = nums.length;
  while (left < right) {
    const mid = Math.floor((left + right) / 2);
    if (nums[mid] < target) left = mid + 1;
    else right = mid;
  }
  return left;
}

// 查找最后一个 <= target 的位置（upper_bound）
function upperBound(nums, target) {
  let left = 0, right = nums.length;
  while (left < right) {
    const mid = Math.floor((left + right) / 2);
    if (nums[mid] <= target) left = mid + 1;
    else right = mid;
  }
  return left - 1;
}
```

---

### Q26: 排序算法对比？

**难度**: ⭐⭐

**答案**:

| 算法 | 平均 | 最坏 | 空间 | 稳定 | 适用场景 |
|------|------|------|------|------|---------|
| 冒泡 | O(n^2) | O(n^2) | O(1) | 是 | 数据量小 |
| 快排 | O(n log n) | O(n^2) | O(log n) | 否 | 通用排序 |
| 归并 | O(n log n) | O(n log n) | O(n) | 是 | 链表、外部排序 |
| 堆排 | O(n log n) | O(n log n) | O(1) | 否 | TopK |
| 插入 | O(n^2) | O(n^2) | O(1) | 是 | 基本有序 |
| 计数 | O(n+k) | O(n+k) | O(k) | 是 | 整数范围小 |

---

### Q27: 在排序数组中查找元素的第一个和最后一个位置（LeetCode 34）

**难度**: ⭐⭐

**答案**: 使用两次二分查找，分别找左边界和右边界。

**解析**:
```javascript
function searchRange(nums, target) {
  const lowerBound = (target) => {
    let left = 0, right = nums.length;
    while (left < right) {
      const mid = Math.floor((left + right) / 2);
      if (nums[mid] < target) left = mid + 1;
      else right = mid;
    }
    return left;
  };
  const start = lowerBound(target);
  if (start === nums.length || nums[start] !== target) return [-1, -1];
  const end = lowerBound(target + 1) - 1;
  return [start, end];
}
```

---

### Q28: 搜索旋转排序数组（LeetCode 33）

**难度**: ⭐⭐⭐

**答案**: 二分查找变体，先判断哪一半是有序的，再判断目标值在有序的那一半还是无序的那一半。

**解析**:
```javascript
function search(nums, target) {
  let left = 0, right = nums.length - 1;
  while (left <= right) {
    const mid = Math.floor((left + right) / 2);
    if (nums[mid] === target) return mid;
    // 判断左半部分是否有序
    if (nums[left] <= nums[mid]) {
      if (target >= nums[left] && target < nums[mid]) right = mid - 1;
      else left = mid + 1;
    } else {
      // 右半部分有序
      if (target > nums[mid] && target <= nums[right]) left = mid + 1;
      else right = mid - 1;
    }
  }
  return -1;
}
// 时间复杂度: O(log n)，空间复杂度: O(1)
```

---

## 五、动态规划

### Q29: 爬楼梯（LeetCode 70）

**难度**: ⭐

**答案**: 到第 n 阶的方法数 = 到第 n-1 阶 + 到第 n-2 阶。斐波那契数列。

**解析**:
```javascript
function climbStairs(n) {
  if (n <= 2) return n;
  let prev1 = 1, prev2 = 2;
  for (let i = 3; i <= n; i++) {
    const curr = prev1 + prev2;
    prev1 = prev2;
    prev2 = curr;
  }
  return prev2;
}
// 时间复杂度: O(n)，空间复杂度: O(1)
```

---

### Q30: 0-1 背包问题

**难度**: ⭐⭐⭐

**答案**: `dp[i][w]` 表示前 i 个物品、容量为 w 时的最大价值。状态转移：选或不选第 i 个物品。

**解析**:
```javascript
function knapsack(weights, values, capacity) {
  const n = weights.length;
  const dp = Array.from({ length: n + 1 }, () => new Array(capacity + 1).fill(0));
  for (let i = 1; i <= n; i++) {
    for (let w = 0; w <= capacity; w++) {
      dp[i][w] = dp[i - 1][w]; // 不选
      if (w >= weights[i - 1]) {
        dp[i][w] = Math.max(dp[i][w], dp[i - 1][w - weights[i - 1]] + values[i - 1]); // 选
      }
    }
  }
  return dp[n][capacity];
}
// 空间优化为一维数组
function knapsackOptimized(weights, values, capacity) {
  const dp = new Array(capacity + 1).fill(0);
  for (let i = 0; i < weights.length; i++) {
    for (let w = capacity; w >= weights[i]; w--) { // 逆序遍历
      dp[w] = Math.max(dp[w], dp[w - weights[i]] + values[i]);
    }
  }
  return dp[capacity];
}
```

---

### Q31: 最长递增子序列（LeetCode 300）

**难度**: ⭐⭐⭐

**答案**: DP 解法 O(n^2)；贪心 + 二分查找 O(n log n)。

**解析**:
```javascript
// 贪心 + 二分查找（推荐）
function lengthOfLIS(nums) {
  const tails = []; // tails[i] = 长度为 i+1 的递增子序列的最小末尾
  for (const num of nums) {
    let left = 0, right = tails.length;
    while (left < right) {
      const mid = Math.floor((left + right) / 2);
      if (tails[mid] < num) left = mid + 1;
      else right = mid;
    }
    if (left === tails.length) tails.push(num);
    else tails[left] = num;
  }
  return tails.length;
}
// 时间复杂度: O(n log n)，空间复杂度: O(n)
```

---

### Q32: 打家劫舍（LeetCode 198）

**难度**: ⭐⭐

**答案**: `dp[i] = max(dp[i-1], dp[i-2] + nums[i])`，不能偷相邻的房屋。

**解析**:
```javascript
function rob(nums) {
  let prev2 = 0, prev1 = 0;
  for (const num of nums) {
    const curr = Math.max(prev1, prev2 + num);
    prev2 = prev1;
    prev1 = curr;
  }
  return prev1;
}
// 时间复杂度: O(n)，空间复杂度: O(1)
```

---

### Q33: 最长公共子序列（LeetCode 1143）

**难度**: ⭐⭐⭐

**答案**: `dp[i][j]` 表示 text1[0..i-1] 和 text2[0..j-1] 的 LCS 长度。

**解析**:
```javascript
function longestCommonSubsequence(text1, text2) {
  const m = text1.length, n = text2.length;
  const dp = Array.from({ length: m + 1 }, () => new Array(n + 1).fill(0));
  for (let i = 1; i <= m; i++) {
    for (let j = 1; j <= n; j++) {
      if (text1[i-1] === text2[j-1]) {
        dp[i][j] = dp[i-1][j-1] + 1;
      } else {
        dp[i][j] = Math.max(dp[i-1][j], dp[i][j-1]);
      }
    }
  }
  return dp[m][n];
}
// 时间复杂度: O(m*n)，空间复杂度: O(m*n)，可优化为 O(min(m,n))
```

---

### Q34: 编辑距离（LeetCode 72）

**难度**: ⭐⭐⭐

**答案**: `dp[i][j]` 表示 word1[0..i-1] 转换为 word2[0..j-1] 的最小操作数。操作：插入、删除、替换。

**解析**:
```javascript
function minDistance(word1, word2) {
  const m = word1.length, n = word2.length;
  const dp = Array.from({ length: m + 1 }, () => new Array(n + 1).fill(0));
  for (let i = 0; i <= m; i++) dp[i][0] = i;
  for (let j = 0; j <= n; j++) dp[0][j] = j;
  for (let i = 1; i <= m; i++) {
    for (let j = 1; j <= n; j++) {
      if (word1[i-1] === word2[j-1]) {
        dp[i][j] = dp[i-1][j-1];
      } else {
        dp[i][j] = 1 + Math.min(
          dp[i-1][j],     // 删除
          dp[i][j-1],     // 插入
          dp[i-1][j-1]    // 替换
        );
      }
    }
  }
  return dp[m][n];
}
// 时间复杂度: O(m*n)，空间复杂度: O(m*n)
```

---

## 六、图与 BFS/DFS

### Q35: 岛屿数量（LeetCode 200）

**难度**: ⭐⭐

**答案**: DFS/BFS 遍历网格，每次发现新岛屿时计数+1，并将该岛屿所有陆地标记为已访问。

**解析**:
```javascript
function numIslands(grid) {
  if (!grid.length) return 0;
  let count = 0;
  const m = grid.length, n = grid[0].length;

  function dfs(i, j) {
    if (i < 0 || i >= m || j < 0 || j >= n || grid[i][j] !== '1') return;
    grid[i][j] = '0'; // 标记已访问
    dfs(i + 1, j);
    dfs(i - 1, j);
    dfs(i, j + 1);
    dfs(i, j - 1);
  }

  for (let i = 0; i < m; i++) {
    for (let j = 0; j < n; j++) {
      if (grid[i][j] === '1') {
        count++;
        dfs(i, j);
      }
    }
  }
  return count;
}
// 时间复杂度: O(m*n)，空间复杂度: O(m*n)（最坏情况递归栈）
```

---

### Q36: 二叉树的层序遍历（LeetCode 102）

**难度**: ⭐⭐

**答案**: BFS 使用队列，逐层遍历。

**解析**:
```javascript
function levelOrder(root) {
  if (!root) return [];
  const result = [], queue = [root];
  while (queue.length) {
    const level = [];
    const size = queue.length;
    for (let i = 0; i < size; i++) {
      const node = queue.shift();
      level.push(node.val);
      if (node.left) queue.push(node.left);
      if (node.right) queue.push(node.right);
    }
    result.push(level);
  }
  return result;
}
```

---

### Q37: 课程表（LeetCode 207）

**难度**: ⭐⭐⭐

**答案**: 拓扑排序（BFS + 入度表）。如果所有课程都能被访问（无环），则可以完成。

**解析**:
```javascript
function canFinish(numCourses, prerequisites) {
  const graph = Array.from({ length: numCourses }, () => []);
  const inDegree = new Array(numCourses).fill(0);

  for (const [course, prereq] of prerequisites) {
    graph[prereq].push(course);
    inDegree[course]++;
  }

  const queue = [];
  for (let i = 0; i < numCourses; i++) {
    if (inDegree[i] === 0) queue.push(i);
  }

  let count = 0;
  while (queue.length) {
    const node = queue.shift();
    count++;
    for (const neighbor of graph[node]) {
      inDegree[neighbor]--;
      if (inDegree[neighbor] === 0) queue.push(neighbor);
    }
  }
  return count === numCourses;
}
// 时间复杂度: O(V+E)，空间复杂度: O(V+E)
```

---

## 七、手撕代码

### Q38: 手写 LRU 缓存

**难度**: ⭐⭐⭐

**答案**: 利用 ES6 Map 的插入顺序特性，get 时删除后重新插入（移到末尾），put 时超过容量删除第一个元素。

**解析**:
```javascript
class LRUCache {
  constructor(capacity) {
    if (capacity <= 0) throw new Error('capacity must be positive');
    this.capacity = capacity;
    this.cache = new Map();
  }

  get(key) {
    if (!this.cache.has(key)) return -1;
    const value = this.cache.get(key);
    this.cache.delete(key);
    this.cache.set(key, value); // 移到末尾（最近使用）
    return value;
  }

  put(key, value) {
    if (this.cache.has(key)) this.cache.delete(key);
    this.cache.set(key, value);
    if (this.cache.size > this.capacity) {
      // 删除最久未使用（第一个元素）
      this.cache.delete(this.cache.keys().next().value);
    }
  }
}

// 时间复杂度: O(1) get 和 put
// 空间复杂度: O(capacity)

// 测试
const lru = new LRUCache(2);
lru.put(1, 1);
lru.put(2, 2);
console.log(lru.get(1));  // 1
lru.put(3, 3);            // 淘汰 key=2
console.log(lru.get(2));  // -1
```

---

### Q39: 手写 TopK 问题（快速选择算法）

**难度**: ⭐⭐⭐

**答案**: 基于快速排序的 partition，每次只递归包含第 K 个元素的那一半。平均 O(n)，最坏 O(n^2)。

**解析**:
```javascript
function topK(nums, k) {
  return quickSelect(nums, 0, nums.length - 1, nums.length - k);
}

function quickSelect(nums, left, right, k) {
  if (left === right) return nums[left];
  const pivotIndex = partition(nums, left, right);
  if (pivotIndex === k) return nums[k];
  if (pivotIndex < k) return quickSelect(nums, pivotIndex + 1, right, k);
  return quickSelect(nums, left, pivotIndex - 1, k);
}

function partition(nums, left, right) {
  const pivot = nums[right];
  let i = left;
  for (let j = left; j < right; j++) {
    if (nums[j] < pivot) {
      [nums[i], nums[j]] = [nums[j], nums[i]];
      i++;
    }
  }
  [nums[i], nums[right]] = [nums[right], nums[i]];
  return i;
}

// 使用最小堆（适合数据量大的场景）
function topKHeap(nums, k) {
  // 下沉操作：维护最小堆性质
  function heapify(heap, i, size) {
    while (true) {
      let smallest = i;
      const left = 2 * i + 1;
      const right = 2 * i + 2;
      if (left < size && heap[left] < heap[smallest]) smallest = left;
      if (right < size && heap[right] < heap[smallest]) smallest = right;
      if (smallest === i) break;
      [heap[smallest], heap[i]] = [heap[i], heap[smallest]];
      i = smallest;
    }
  }

  // 建堆：从最后一个非叶子节点开始下沉
  function buildHeap(heap) {
    for (let i = Math.floor(heap.length / 2) - 1; i >= 0; i--) {
      heapify(heap, i, heap.length);
    }
  }

  const minHeap = nums.slice(0, k);
  buildHeap(minHeap);
  for (let i = k; i < nums.length; i++) {
    if (nums[i] > minHeap[0]) {
      minHeap[0] = nums[i];
      heapify(minHeap, 0, k);
    }
  }
  return minHeap.sort((a, b) => a - b);
}
```

---

### Q40: 手写并发安全队列（生产者-消费者模式）

**难度**: ⭐⭐⭐

**答案**: 使用 Promise + 异步迭代器实现并发控制的生产者-消费者队列。

**解析**:
```javascript
class AsyncQueue {
  constructor(concurrency = 1) {
    this.concurrency = concurrency;
    this.running = 0;
    this.queue = [];
  }

  async add(task) {
    return new Promise((resolve, reject) => {
      this.queue.push({ task, resolve, reject });
      this.run();
    });
  }

  async run() {
    while (this.running < this.concurrency && this.queue.length > 0) {
      const { task, resolve, reject } = this.queue.shift();
      this.running++;
      try {
        const result = await task();
        resolve(result);
      } catch (err) {
        reject(err);
      } finally {
        this.running--;
        this.run(); // 处理下一个任务
      }
    }
  }
}

// 使用
const queue = new AsyncQueue(3); // 最多 3 个并发

async function fetchUrl(url) {
  const res = await fetch(url);
  return res.json();
}

const results = await Promise.all([
  queue.add(() => fetchUrl('/api/1')),
  queue.add(() => fetchUrl('/api/2')),
  queue.add(() => fetchUrl('/api/3')),
  queue.add(() => fetchUrl('/api/4')),
  queue.add(() => fetchUrl('/api/5')),
]);
// 最多 3 个请求同时执行
```

---

## 高频考点速查表

| 考点 | 出现频率 | 核心要点 |
|------|---------|---------|
| 双指针 | ★★★★★ | 对撞指针、快慢指针、滑动窗口 |
| 滑动窗口 | ★★★★ | 固定窗口、可变窗口、前缀和 |
| 链表操作 | ★★★★ | 反转、合并、快慢指针、哑节点 |
| 二叉树遍历 | ★★★★★ | 前中后序（递归+迭代）、层序 |
| BST | ★★★★ | 中序有序、LCA、第K小 |
| 快排/归并 | ★★★★ | 分治思想、O(n log n) |
| 二分查找 | ★★★★ | 有序数组、边界条件 |
| 动态规划 | ★★★★★ | 状态定义、转移方程、初始化 |
| 背包问题 | ★★★★ | 0-1 背包、完全背包 |
| 最长子序列 | ★★★★ | LIS、LCS、编辑距离 |
| BFS/DFS | ★★★★ | 图遍历、拓扑排序 |
| LRU 缓存 | ★★★★★ | Map 插入顺序，O(1) 操作 |
| TopK | ★★★★ | 快速选择、最小堆 |
